import 'package:flutter/cupertino.dart';

class TikiButton extends StatelessWidget {
  const TikiButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
